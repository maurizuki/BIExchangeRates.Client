info:
  name: Currencies
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUri}}/currencies?lang=en"
  params:
    - name: lang
      value: en
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
